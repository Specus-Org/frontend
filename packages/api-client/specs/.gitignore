# Specs fetched from the backend repo (regenerated by `pnpm run fetch-openapi`)
openapi.yaml
auth.yaml
cms-admin.yaml
cms-public.yaml

# Keep the local admin-auth spec (not in backend repo)
!admin-auth.yaml
