{
  "name": "specus",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "api": "pnpm --filter @specus/api-client run api",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "prettier": "catalog:",
    "turbo": "catalog:"
  },
  "packageManager": "pnpm@10.15.1"
}
